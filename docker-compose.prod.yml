version: "3.9"

services:

  fastapi:
    build:
      context: .
      dockerfile: docker/fastapi.Dockerfile

    ports:
      - "8000:8000"

    restart: always

  streamlit:
    build:
      context: .
      dockerfile: docker/streamlit.Dockerfile

    ports:
      - "8501:8501"

    restart: always